{
  "name": "netrai-obsidian",
  "version": "1.0.0",
  "private": true,
  "description": "NetraAI - AI-Powered Healthcare Platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "install": "cd frontend && npm install"
  },
  "workspaces": [
    "frontend"
  ]
}
