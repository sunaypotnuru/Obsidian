# ============================================================
# NETRA AI - MACHINE LEARNING REQUIREMENTS
# ============================================================
# Heavy ML dependencies for the anemia detection service
# ============================================================

# ── CORE API DEPENDENCIES ──────────────────────────────────
# TensorFlow 2.13 pins `typing-extensions <4.6`, which conflicts with FastAPI (Pydantic v2).
# The anemia service does not require Pydantic v2 features, so we keep FastAPI on the
# Pydantic v1 line for a stable, Docker-friendly install.
fastapi==0.95.2
uvicorn[standard]==0.22.0
python-multipart==0.0.6
httpx==0.27.0
jinja2>=3.1.2
pydantic==1.10.13

# ── IMAGE PROCESSING & COMPUTER VISION ─────────────────────
Pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0
scikit-learn==1.3.0
pandas==2.0.3
matplotlib==3.7.2

# ── MACHINE LEARNING & AI ──────────────────────────────────
tensorflow==2.13.0
mediapipe==0.10.7